<%= form_with(model: [commentable, comment], local: true) do |form| %>

<%= render 'layouts/error_explanation', model: commentable %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
